---
- hosts: hostlist
  become: yes
  tasks:
    - name: Change user password
      user:
        #name: root
        name: "{{ account }}"
        update_password: always
        password: "{{ newpassword|password_hash('sha512') }}"
